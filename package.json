{
  "name": "reilley.dev",
  "version": "0.0.1",
  "description": "",
  "license": "",
  "author": "",
  "scripts": {
    "build": "parcel build index.html",
    "deploy": "aws s3 sync dist/ s3://www.reilley.dev",
    "dev": "parcel index.html --open",
    "start": "npm run build && npm run dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@fontsource/roboto-mono": "^4.2.1",
    "sanitize.css": "^12.0.1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "prettier": "^2.2.1"
  },
  "prettier": {
    "printWidth": 120
  }
}
